version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=postgresql://user:password@db:5432/supplyprescript
      - SECRET_KEY=your_secret_key
      - DEBUG=True
    depends_on:
      - db

  db:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: supplyprescript
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: